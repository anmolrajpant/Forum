<% if @results %>
  <% @results.each do |post| %>
	  <tr>
	    <td><%= post.title %></td><br>
	    <td><%= post.comment %></td><br>
	  </tr>
	 <% end %>
<% else %>
  		<tr>
		  <td>No posts</td>
	  	</tr>
<% end %>